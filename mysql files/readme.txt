
create two databases named alumni, alumni_db.


Place the .sql files in an easy location (e.g., Desktop).
Open Command Prompt or Terminal in that location.

Run:

mysql -u root -p alumni < alumni.sql
mysql -u root -p alumni_db < alumni_db.sql
